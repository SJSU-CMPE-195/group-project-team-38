# Unit Tests

Current unit-focused coverage lives in `packages/backend/tests/ai.test.ts`.

What is covered:

- AI provider selection
- model selection
- missing/blank key handling
- invalid configuration behavior
