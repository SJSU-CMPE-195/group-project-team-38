{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install -g bun@1.3.7 && while [ ! -f package.json ] && [ \"$PWD\" != \"/\" ]; do cd ..; done; bun install",
  "buildCommand": "while [ ! -f turbo.json ] && [ \"$PWD\" != \"/\" ]; do cd ..; done; bun turbo run build --filter=web",
  "outputDirectory": ".next"
}
