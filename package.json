{
  "name": "meditag",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "src/apps/*",
    "src/packages/*"
  ],
  "scripts": {
    "convex:dev": "pnpm --filter api dev",
    "mobile:dev": "pnpm --filter mobile start",
    "admin:dev": "pnpm --filter admin dev"
  }
}
